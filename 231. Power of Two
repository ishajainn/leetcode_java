class Solution {
    public boolean isPowerOfTwo(int n) {
        int r=0;
        while(Math.pow(2,r)<=n){
            if(Math.pow(2,r)==n){
                    return true;
            }
            r++;
            
        }
        return false;
}}
