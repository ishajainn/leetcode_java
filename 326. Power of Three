class Solution {
    public boolean isPowerOfThree(int n) {
        int r=0;
        while(Math.pow(3,r)<=n){
            if(Math.pow(3,r)==n){
                    return true;
            }
            r++;
            
        }
        return false;
}}
