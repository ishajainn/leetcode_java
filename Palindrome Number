class Solution {
    public boolean isPalindrome(int x) {
        int n=x;
        int i=0;
        int c=0;
        while(n>0){
            int nes=n%10;
            c=c*10+nes;
            n=n/10;
            i++;
        }
            return x==c;
    }
}
