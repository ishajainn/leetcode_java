class Solution {
    public boolean isPowerOfFour(int n) {
        int r=0;
        while(Math.pow(4,r)<=n){
            if(Math.pow(4,r)==n){
                    return true;
            }
            r++;
            
        }
        return false;
}}
